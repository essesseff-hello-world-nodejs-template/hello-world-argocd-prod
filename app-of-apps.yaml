apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-world-argocd-prod
  namespace: argocd
  labels:
    app: hello-world
    environment: prod
    essesseff: "true"
spec:
  project: default
  source:
    repoURL: https://github.com/essesseff-hello-world-go-template/hello-world-argocd-prod
    targetRevision: main
    path: argocd
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

